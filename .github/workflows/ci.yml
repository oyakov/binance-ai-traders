name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build shared model
        working-directory: binance-shared-model
        run: ./mvnw -B -ntp clean install

      - name: Run service test suites
        run: ./mvnw -B -ntp -am -pl binance-data-collection,binance-data-storage,binance-trader-grid,binance-trader-macd clean test

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Telegram frontend dependencies
        working-directory: telegram-frontend-python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-test.txt
          pip install -e .

      - name: Run Telegram frontend tests
        working-directory: telegram-frontend-python
        run: python -m unittest discover -s tests -p '*_test.py'
